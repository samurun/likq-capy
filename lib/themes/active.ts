// Active theme. Swap the import below to switch which quiz the app serves.
// All engine modules (lib/quiz/*, OG routes, sitemap, etc.) read through
// this re-export, so changing the line is enough — no other code edits.
export { theme as activeTheme } from "./capybara-cozy"
